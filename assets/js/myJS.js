let typed2 = new Typed('.works', {
    strings: ['<i>Web Developer</i>', '<i>PegaSystem Developer</i>', '<i>BackEnd Developer</i>'],
    typeSpeed: 40,
    backSpeed: 40,
    loop: true,
    showCursor: true,
    smartBackspace: true
});

const myChart = $(".php").donutty({
    min: -100,
    max: 100,
    value: 50
});